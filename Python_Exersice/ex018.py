'''
    Make a program that calculates sine, cosine and tangent
'''
from math import sin, cos, tan

angle = float(input("Enter a number: "))

print(f"This is the result; sine: {sin(angle):.2f}; cosine: {cos(angle):.2f} and tangent: {tan(angle):.2f}")
